# Copy to .env and fill in. .env is gitignored — never commit it.

# Supabase > Project Settings > Data API > Project URL
SUPABASE_URL=https://xxxxxxxxxxxxxxxx.supabase.co

# Supabase > Project Settings > API Keys > Secret keys.
# Starts with sb_secret_. This is the replacement for the legacy service_role
# JWT. Server-side only: it bypasses Row Level Security entirely and must
# never reach the mobile app.
SUPABASE_SECRET_KEY=

# Supabase Storage bucket name (created automatically on boot if missing)
SUPABASE_BUCKET=audio

# Sarvam dashboard > API Keys
SARVAM_API_KEY=

PORT=8787
